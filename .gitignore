# Generated plugin archive
dist/
dist-internal/

# Python validation caches
__pycache__/
*.py[cod]
.coverage
.coverage.*
coverage.json
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Python virtual environments
.venv/
venv/

# Secrets / credentials — defence in depth. .gitattributes marks these as
# binary if they ever sneak in, but the repo must be tolerant of an
# accidental .env file being committed (.gitignore catches it pre-commit).
# The .gitattributes binary rules cover only the file patterns below, not the
# secrets/ directory.
.env
.env.*
*.pem
*.key
secrets/

# Editor / OS
.idea/
.vscode/
.DS_Store
*.swp
*.swo
*~
Thumbs.db

# Athena-specific: skill dumps from local iteration
skills/_out/
skills/_drafts/

# Local plugin scanner output (the scanner writes here; we do not commit)
.scanner-out/
